Production artifact validation:

The generated FACTORY_ARTIFACT.json must derive its goal from the generated SPEC.json purpose so push-triggered production runs cannot publish the placeholder `default production goal`.
